<form method="post" action="{{ path('app_ia_delete', {'id': ium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ium.id) }}">
    <button class="btn">Delete</button>
</form>
