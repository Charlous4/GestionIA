<form method="post" action="{{ path('app_ingenieur_delete', {'id': ingenieur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ingenieur.id) }}">
    <button class="btn">Delete</button>
</form>
