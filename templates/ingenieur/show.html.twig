{% extends 'base.html.twig' %}

{% block title %}Ingenieur{% endblock %}

{% block body %}
    <h1>Ingenieur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ingenieur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ ingenieur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ ingenieur.prenom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ingenieur_index') }}">back to list</a>

    <a href="{{ path('app_ingenieur_edit', {'id': ingenieur.id}) }}">edit</a>

    {{ include('ingenieur/_delete_form.html.twig') }}
{% endblock %}
